#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

#launch vmware view from thinpro with USB support and better audio latency
export VMWARE_DISABLE_PULSE_AUDIO_SUPPORT=1
[ -x /usr/lib/vmware/vmware-usbarbitrator ] && /usr/lib/vmware/vmware-usbarbitrator
[ -x /usr/lib/vmware/vmware-view-usbd ] && /usr/lib/vmware/vmware-view-usbd
# If you want to use GNOME config tools...
#
#if test -x /usr/lib/openbox/gnome-settings-daemon >/dev/null; then
#  /usr/lib/openbox/gnome-settings-daemon &
#elif which gnome-settings-daemon >/dev/null; then
#  gnome-settings-daemon &
#fi

# If you want to use XFCE config tools...
#
#xfce-mcs-manager &
hsetroot -fill /root/vmview.png
x11vnc -rfbauth /root/.config/openbox/.vncpass -rfbport 23245  -forever -bg
/root/vmview &
